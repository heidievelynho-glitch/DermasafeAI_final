services:
  - type: web
    name: dermasafeai
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.6
      - key: ENVIRONMENT
        value: production
